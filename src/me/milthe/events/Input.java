package me.milthe.events;

import javafx.scene.input.KeyCode;

import java.awt.event.KeyEvent;

public class Input{

    //KEYCODE W = 0, A = 1, S
    private boolean[] pressed;

    public Input(){
        pressed = new boolean[5];
        getKeycode(KeyCode.SPACE);
    }

    public short getKeycode(KeyCode code){
        if ()
        return 0;
    }
}
